{
  "name": "sign-chat-server",
  "version": "1.0.0",
  "scripts": {
    "client": "cd .. && cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
